# /kemaskini.html is deliberately NOT Disallow'd here (2026-08-08). It carries
# <meta name="robots" content="noindex, nofollow"> instead. It is linked from
# index.html and school.html, and a page that is blocked from crawling but
# linked to still gets its URL indexed -- GSC reported exactly that. A crawler
# has to be able to FETCH a page to obey its noindex. Do not re-add it here.

User-agent: *
Allow: /
Disallow: /admin.html
Disallow: /admin
Disallow: /api/

# Block known aggressive scrapers and AI training bots
# that don't respect data ownership
User-agent: GPTBot
Disallow: /

User-agent: CCBot
Disallow: /

User-agent: anthropic-ai
Disallow: /

User-agent: Claude-Web
Disallow: /

User-agent: FacebookBot
Disallow: /

# Meta's AI-training crawler (feeds Llama), separate from FacebookBot above
# and separate from facebookexternalhit (the link-preview fetcher used when
# a CariSchool link is shared on Facebook/Instagram/WhatsApp/Messenger --
# that one is intentionally NOT listed here, so it stays allowed under the
# "User-agent: *" group at the top and shared links keep working). Added
# 2026-09-17 after confirming FacebookBot and meta-externalagent are two
# distinct Meta crawlers, not the same bot under two names.
User-agent: meta-externalagent
Disallow: /

User-agent: Meta-ExternalAgent
Disallow: /

User-agent: Bytespider
Disallow: /

User-agent: PetalBot
Disallow: /

User-agent: SemrushBot
Disallow: /

User-agent: AhrefsBot
Disallow: /

User-agent: MJ12bot
Disallow: /

User-agent: DotBot
Disallow: /

# Google-Extended intentionally left unblocked (2026-07-19): this is the
# toggle that controls Google AI Overviews eligibility specifically, not
# regular Googlebot search indexing (which was already allowed). Since
# Google Search is CariSchool's dominant real traffic source, keeping this
# blocked was costing real visibility for no clear benefit -- unlike
# GPTBot/CCBot/Bytespider/PetalBot below, which are training-data crawlers
# for products unrelated to how parents actually find CariSchool today.

# ── AI SEARCH crawlers: explicitly allowed (2026-08-07) ──
# These were already allowed by the "User-agent: *" group at the top, so this
# changes no behaviour. It is written out so the intent is unmistakable and
# cannot be undone by accident: the blocks above target TRAINING crawlers
# (GPTBot, CCBot, anthropic-ai, Claude-Web, Bytespider, meta-externalagent),
# and each vendor runs separate agents for search retrieval and citation.
# Blocking one and allowing the other is deliberate, and the pairs are easy
# to confuse -- GPTBot is not OAI-SearchBot, Claude-Web is not ClaudeBot,
# meta-externalagent is not Meta-ExternalFetcher.
#
# This list matches the crawler regex in vercel.json, which routes these same
# agents to /api/prerender for no-JavaScript HTML. If an agent is added there,
# add it here too, or it gets prerendered content it was never allowed to fetch.
#
# Note: "Disallow: /api/" below does NOT block the prerender route. That route
# is reached by a server-side rewrite from /kawasan.html and /school/:slug, so
# the URL a crawler actually requests is never under /api/. Do not "fix" this.

User-agent: OAI-SearchBot
Allow: /
Disallow: /admin.html
Disallow: /admin
Disallow: /api/

User-agent: ChatGPT-User
Allow: /
Disallow: /admin.html
Disallow: /admin
Disallow: /api/

User-agent: PerplexityBot
Allow: /
Disallow: /admin.html
Disallow: /admin
Disallow: /api/

User-agent: Perplexity-User
Allow: /
Disallow: /admin.html
Disallow: /admin
Disallow: /api/

User-agent: ClaudeBot
Allow: /
Disallow: /admin.html
Disallow: /admin
Disallow: /api/

User-agent: Claude-SearchBot
Allow: /
Disallow: /admin.html
Disallow: /admin
Disallow: /api/

User-agent: Claude-User
Allow: /
Disallow: /admin.html
Disallow: /admin
Disallow: /api/

# Meta AI's real-time citation/answer-retrieval crawler -- same category as
# OAI-SearchBot/PerplexityBot above, not a training crawler. Added
# 2026-09-17: Malaysia has very heavy WhatsApp/Meta AI usage, so allowing
# CariSchool to be cited in Meta AI answers is worth the same tradeoff
# already made for OpenAI/Perplexity/Anthropic's retrieval bots.
User-agent: Meta-ExternalFetcher
Allow: /
Disallow: /admin.html
Disallow: /admin
Disallow: /api/

# Allow legitimate search engines full access
User-agent: Googlebot
Allow: /
Disallow: /admin.html
Disallow: /admin
Disallow: /api/

User-agent: Bingbot
Allow: /
Disallow: /admin.html
Disallow: /admin
Disallow: /api/

Sitemap: https://www.carischools.com/sitemap.xml
